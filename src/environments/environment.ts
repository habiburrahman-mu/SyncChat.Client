import { Environment } from './environment.model';

export const environment: Environment = {
  production: false,
  apiBaseUrl: 'https://localhost:5001/api',
  notificationHubUrl: 'https://localhost:5001/hub/notifications',
};
