import { SystemMessageAsyncPipe } from './system-message-async.pipe';

describe('SystemMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new SystemMessageAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
