import { SystemMessagePipe } from './system-message.pipe';

describe('SystemMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new SystemMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
